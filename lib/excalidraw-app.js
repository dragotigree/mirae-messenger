/* 692에서 자체 그림판으로 대체되어 제거됐던 파일. 1.0.691 이전 버전의 main.js는
   이 파일을 "필수 업데이트 파일" 목록에 아직 갖고 있어서, 파일이 저장소에 없으면
   업데이트 전체가 404 오류로 막혀버린다(실제 발생). 그 구버전들이 새 버전으로
   넘어올 수 있도록 최소 내용만 남겨둔다. 1.0.691+ 는 이 파일을 아예 쓰지 않는다. */
