{
  "version": "1.0.527",
  "notes": "chat_pins류 스키마 국소 충돌이 얕은 재생성(DROP→CREATE)으로 안 고쳐지고 재발하는 경우 자동 복구 추가 — DROP도 손상된 sqlite_master를 먼저 읽어야 해서 조용히 실패하고, 그 여파로 상관없는 테이블(hospital_schedules 등) 조회까지 같은 오류로 계속 실패해 현황판/일정이 안 뜨던 문제. writable_schema로 sqlite_master 중복 행을 직접 정리하는 저수준 복구를 추가하고, 그마저 실패하면 백업 복구로 넘어간다."
}
